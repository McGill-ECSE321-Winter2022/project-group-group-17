name: Java CI with Gradle

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: Windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 17
      uses: actions/setup-java@v2
      with:
        java-version: '17'
        distribution: 'adopt'
        cache: gradle
    - name: Validate Gradle wrapper
      uses: gradle/wrapper-validation-action@e6e38bacfdf1a337459f332974bb2327a31aaf4b
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      working-directory: GroceryStoreSystem-Backend
    - name: Build with Gradle
      run: ./gradlew build
      working-directory: GroceryStoreSystem-Backend

  frontend:
      needs: build
      runs-on: Windows-latest
      steps:
        - uses: actions/checkout@v2
        - name: push frontend
    
          with:
            heroku_api_key: 64203088-2e45-4dcd-a709-d1f5f42866e5
            heroku_app_name: "grocerystoresys-frontend-22ws" #Must be unique in Heroku
            heroku_email: "onyekachi.ezekwem@mail.mcgill.ca"
            dontuseforce: false
